<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team14.project110">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Course_Activity"
            android:label="@string/title_activity_course_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Professor_Activity" />
        <activity android:name=".testCourse" />
        <activity android:name=".testProfessor" />
        <activity android:name=".testLecture" />
        <activity android:name=".testNote"></activity>
    </application>

</manifest>
